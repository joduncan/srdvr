require 'rspec'
require_relative '../recorder'

describe 'Recorder class' do

  # this test class could possibly use Ruby timeouts to verify that the start/stop calls don't take
  # longer than X seconds (or milliseconds) to execute. I'm skipping these tests right now because
  # that's outside the scope of the work I want to do for this example.

  # it 'should start recording without blocking' do
  # end

  # it 'should stop recording without blocking' do
  # end

end